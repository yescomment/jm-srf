<footer>
  <nav>
    <div>
      <slot name="contact"/>
    </div>
    <div class="site-nav">
      <slot name="sitenav">
    </div>
    <div>
      <slot name="othersite">
    </div>
  </nav>
</footer>

<style lang="scss">
  footer {
    padding: 4rem;
    background-color: var(--color-primary);
    background: linear-gradient(var(--color-primary-tinted), var(--color-primary));

    nav {
      max-width: 1200px;
      display: grid;
      gap: 4rem;
      grid-template-columns: repeat(3, 1fr);

      :global(a, hr) {
        display: block;
        margin-block: 1rem;
        text-decoration: none;
        color: white;
      }

      :global(a:hover) {
        color: var(--color-light);
      }

      @media (max-width: 500px) {
        grid-template-columns: 1fr;
      }
    }

    :global(.button) {
      width: 10rem;
      padding: 1rem;

      &:hover {
        box-shadow: 0 0 5px var(--color-dark);
      }
    }

    :global(.to-srfcapital) {
      color: var(--color-light);
      background-color: #223749; // --color-primary
      &:hover {
        background-color: #223749; // --color-primary-tinted
      }
    }

    :global(.to-srfimpact) {
      color: var(--color-light);
      background-color: #e74c34; // --color-primary
      &:hover {
        background-color: #c93d2f; // --color-primary-tinted
      }
    }
  }
</style>